[build]
builder = "dockerfile"
dockerfilePath = "landing/Dockerfile"
watchPatterns = ["landing/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 5
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
